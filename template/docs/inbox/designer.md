# Inbox — designer(设计方收件箱)

> 只放待办。处理完的条目由处理方删除并随工作 commit;历史在 git log。
> 条目 = 指针 + 摘要,全文见对应 docs/ 产出目录。
> 提请条件(写码方/审阅方):仅限 spec 缺失关键行为定义、规则互相矛盾、验收标准无法客观判定。
> 实现细节 HOW、代码质量问题不提请;"我觉得这样更好"可附注但不阻塞。
> **处理顺序**:裁定请求永远先于一切待办——每单裁定都堵着一个在制品 PR;待办(出新 spec)受 coder FIFO 水线约束,不抢时效。

(当前无待办)
